{
  "name": "harness",
  "version": "0.1.0",
  "private": true,
  "description": "Claude Code ハーネス育成アプリ（個人用・Hub 集約型）",
  "packageManager": "pnpm@11.13.1",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "build": "pnpm -r build",
    "db:init": "pnpm --filter @harness/shared exec tsx src/init-db.ts",
    "typecheck": "pnpm --filter @harness/shared build && pnpm -r run typecheck"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.4",
    "tsx": "^4.23.1",
    "typescript": "^5.9.3"
  }
}
